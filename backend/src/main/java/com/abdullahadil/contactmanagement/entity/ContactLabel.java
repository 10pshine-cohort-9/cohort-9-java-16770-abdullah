package com.abdullahadil.contactmanagement.entity;

/**
 * Shared label used to tag both contact emails and contact phone numbers
 * (e.g. "work", "home", "personal") per the contact profile requirements.
 */
public enum ContactLabel {
    WORK,
    HOME,
    PERSONAL,
    OTHER
}
